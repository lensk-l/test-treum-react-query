import {products} from "../../../data/data";export default function handler(req, res) {    if (req.method === 'GET') {        if (req.query) {            let filteredProducts = [...products]            const {amount, category} = req.query;            if (amount) {                filteredProducts = filteredProducts.filter(product => product.amount <= Number(amount));            }            if (category) {                filteredProducts = filteredProducts.filter(product => product.category === category);            }            return res.status(200).json(filteredProducts)        }        res.status(200).json(products)    }    if (req.method === 'POST') {        const body = req.body        const newProduct = {            id: Date.now(),            category: body.category,            title: body.title,            amount: body.amount,            action: 'Міксер у подарунок',            tag: 'Лідер з продажу',            img: "https://e7.pngegg.com/pngimages/305/107/png-clipart-lg-electronics-led-backlit-lcd-computer-monitors-home-appliance-lg-text-trademark.png",            advantages: [                'Безкоштовна доставка',                'Є можливість кредитування',                'Гарантія 3 роки'            ]        }        products.push(newProduct);        res.status(201).json(products)    }}
import SearchBlock from "../components/search-block/SearchBlock";import ProductsList from "../components/products-list/ProductsList";import {useSelector} from "react-redux";import {useEffect, useState} from "react";const  Index= () => {    const products = useSelector((state) => state.rootReducers.products.productsAll);    const [allProducts, setProducts] = useState();    const [filteredProducts, setFilteredProducts] = useState(allProducts)    useEffect(()=> {        setProducts(products);    },[allProducts])    // useEffect(()=> {handlerSearch()}, [allProducts])    //    // const handlerSearch = (searchInput, categoryValue) => {    //     let data = [...allProducts]    //    //     // if(categoryValue) {    //     //     data = data.filter(product => product.category.includes(categoryValue))    //     // }    //     // if(searchInput) {    //     //     data = data.filter(product => product.price < searchInput)    //     // }    //    //     setFilteredProducts(data);    // }    return (        <>            <SearchBlock                // onSearch={handlerSearch}            />            <ProductsList countOfProducts={products.length} products={allProducts}  />        </>    );}export default Index;